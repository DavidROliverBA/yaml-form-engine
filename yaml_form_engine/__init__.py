"""YAML Form Engine — Define forms in YAML, render them in Streamlit.

All form data stays local. The AI generates the YAML config;
humans fill in the data on their own machine.
"""

__version__ = "0.1.0"
